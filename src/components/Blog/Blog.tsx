import { ContainerStyled } from "../Container";
import { Header } from "../Header";

export const Blog = () => {
  return (
    <ContainerStyled>
      <Header />

    </ContainerStyled>
  );
};

export default Blog;
